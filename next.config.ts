import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Netlify supports Next.js API routes as serverless functions
  // No need for static export - full Next.js support
};

export default nextConfig;
